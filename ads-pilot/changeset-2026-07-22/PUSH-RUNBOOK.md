# PUSH-RUNBOOK — execute the Google Ads bus-fix (blind-executable)

You are a fresh session. This file + the JSON files beside it are your entire brief. Read this fully, then execute top to bottom. Everything you need (customer id, campaign/group/ad ids, GAQL, exact mutation bodies) is inlined or generated by the scripts here. **Do not improvise beyond this file.**

## What this run does (Noam's TIGHTENED LAW, 2026-07-23)
Replace the 8 old RSAs in dad's bus campaign with 4 fresh RSAs (one per ad group) where **every headline contains the literal word אוטובוס**, **REMOVE** the 8 old ads (Noam: "תמחק את המודעות הלא טובות" — delete, not pause), add 3 exact-match price keywords (all contain אוטובוס), **PAUSE every live keyword that does not contain אוטובוס** (30 keywords) **plus the one job-seeker keyword `חיפוש עבודה נהג אוטובוס`** (contains אוטובוס but is pure waste — foreman ruling 2026-07-23, Noam's intent outranks the mechanical letter) = **31 pauses**; the ad should only show on genuine bus-hire searches, add the negatives that pass the self-block + dedup checks (**including מיניבוס — the fleet is ONE 56-seat bus, no minibus**), optionally cap CPC at ₪8 if bidding is Maximize Clicks, then — **ONLY after the post-verify bus-word assertion passes — ENABLE the campaign as the final mutation** (Noam's explicit approval 2026-07-23: "תתקן הכל כולל להפעיל את הפרסומת").

**REVOKED:** the old "keep the campaign PAUSED / re-enable is a separate Noam money-tap" law is GONE. Noam approved the enable. The enable is now the last step of this run, gated only on post-verify.

## Fixed facts (do not re-derive)
- `customer_id` = **1128064207**
- Search campaign = **24022931741** ("אבי ורדי — חיפוש") — currently **PAUSED**; this run ENABLES it at the very end.
- Ad groups: טיולים ואירועים=`197000680966` · הסעות לעובדים וכללי=`198995179115` · חתונות=`201158533351` · נתב״ג=`202032774927`
- 8 old ad resourceNames (adGroupId~adId), all to **REMOVE**:
  `197000680966~816419150547` · `197000680966~816438019647` · `198995179115~816438019650` · `198995179115~816454544560` · `201158533351~816438019644` · `201158533351~816527498297` · `202032774927~816438019641` · `202032774927~816454215061`
- Final URL for new RSAs: `https://www.avivardi.online`
- Working dir (cwd): `~/Claude/Avi-vardi/ads-pilot/changeset-2026-07-22/`
- All 31 keyword pauses are in group `202032774927` (נתב״ג) — the 30 non-אוטובוס keywords + the 1 job-seeker exception `חיפוש עבודה נהג אוטובוס`.

## Tool routes
- **READ (GAQL):** `mcp__composio-headless__GOOGLEADS_SEARCH_STREAM_GAQL`, param `customer_id="1128064207"`, one call per query. (Interactive fallback: `mcp__composio__COMPOSIO_MULTI_EXECUTE_TOOL` batching the same GAQL.)
- **WRITE (mutations):** `mcp__composio__COMPOSIO_REMOTE_WORKBENCH` → `proxy_execute(toolkit="googleads", method="POST", endpoint=<the _endpoint in the payload>, body=<the op body>)`. v21 is proven; if a call 404s specifically on v21, retry the identical call on v23 and note the bump. (Native fallback if the proxy is unavailable: `GOOGLEADS_MUTATE_AD_GROUP_ADS` / `GOOGLEADS_MUTATE_AD_GROUP_CRITERIA` / `GOOGLEADS_MUTATE_CAMPAIGN_CRITERIA` / `GOOGLEADS_MUTATE_CAMPAIGNS` — but the REST bodies below are the primary path.)
- **Every mutation, no exceptions:** send with `"validateOnly": true` first (empty `{}` response = PASS, not failure) → then send the identical body without `validateOnly` → then an independent fresh GAQL re-read confirming it landed → then append one line to the ops log. If validateOnly fails: STOP that operation, log a `warn` alert, continue the rest — EXCEPT the campaign ENABLE, which is hard-gated on post-verify (see Step 4/6).

---

## STEP 0 — quota probe (abort-early)
Run this GAQL:
```
SELECT campaign.id, campaign.status, campaign.bidding_strategy_type FROM campaign WHERE campaign.id = 24022931741
```
- On **429 / RESOURCE_EXHAUSTED**: quota still blocked. Do NOTHING else. Notify Noam and STOP:
  `node ~/.claude/hooks/tg-notify.js "Ads bus-fix push: quota still 429 at $(date). Not pushed — will retry next window."`
- On success: note the returned `campaign.status` (expected `PAUSED`) and **record `campaign.bidding_strategy_type`** — you need it for Step 3f. If status already reads `ENABLED`, someone enabled it early; proceed with the changeset but you can skip Step 3h (already enabled) — note it in the report.

## STEP 1 — archive pull (before any change)
Run these and save the combined JSON to `archive-<TODAY>.json` in the cwd (e.g. `archive-2026-07-23.json`):

```
-- campaign settings: status, bidding strategy, budget (for the bidding-ceiling decision + budget confirmation)
SELECT campaign.id, campaign.name, campaign.status, campaign.bidding_strategy_type,
  campaign_budget.amount_micros
FROM campaign WHERE campaign.id = 24022931741

-- current RSA content (for the archive + the bus-word audit)
SELECT ad_group.name, ad_group_ad.ad.id, ad_group_ad.status,
  ad_group_ad.ad.responsive_search_ad.headlines, ad_group_ad.ad.responsive_search_ad.descriptions
FROM ad_group_ad WHERE campaign.id = 24022931741

-- itemized live negatives (dedup source for Step 3e)
SELECT campaign_criterion.criterion_id, campaign_criterion.keyword.text, campaign_criterion.keyword.match_type
FROM campaign_criterion WHERE campaign.id = 24022931741 AND campaign_criterion.type = 'KEYWORD'

-- live positive keywords WITH criterion_id (needed to map pause targets + re-run self-block)
SELECT ad_group.name, ad_group.id, ad_group_criterion.criterion_id,
  ad_group_criterion.keyword.text, ad_group_criterion.keyword.match_type, ad_group_criterion.status
FROM keyword_view WHERE campaign.id = 24022931741
```
**AUDIT (write into your final report):** from the RSA query, list which OLD headlines did NOT contain אוטובוס — that's the "before" evidence for Noam that the bus-word rule was being violated. Also record the budget (must read ₪30/day = 30,000,000 micros — you never change it) and the bidding_strategy_type.

## STEP 2 — recompute the pause set + re-run self-block against the FRESH pull
Do not trust the snapshot. From the Step-1 keyword pull:
1. **Pause set** = every live positive keyword whose text does NOT contain `אוטובוס`, **PLUS the explicit exception `חיפוש עבודה נהג אוטובוס`** (job-seeker waste — foreman ruling; it contains אוטובוס but is paused anyway). This is what `keyword-pauses.json` holds (31 keywords: 30 non-אוטובוס + 1 exception as of the snapshot). If the live pull differs (a keyword added/removed since), the LIVE non-אוטובוס set + the exception wins — pause exactly those. Map each to its `criterion_id` by exact text.
2. **Surviving active set** = the live positives that DO contain `אוטובוס` (what remains after the pauses).
3. **Self-block** each negative candidate (decision `add`/`add-if-absent`) against the **surviving active set**, not the full pre-pause list — because the pauses in Step 3d land BEFORE the negatives in Step 3e:
```
# save the surviving-active keyword texts (those containing אוטובוס, MINUS the paused exception חיפוש עבודה נהג אוטובוס) as a JSON array to active-positives.json, then:
node -e '
const fs=require("fs");
const pos=JSON.parse(fs.readFileSync("active-positives.json","utf8"));   // live keyword.text containing אוטובוס
const negs=JSON.parse(fs.readFileSync("negatives.json","utf8"));
for(const n of negs.filter(x=>x.decision!=="skip")){
  const hits=pos.filter(p=>p.includes(n.text));
  console.log((hits.length?"SELF-BLOCK->DROP ":"CLEAR         ")+n.text+(hits.length?"  hits:"+hits.join(","):""));
}'
```
Any candidate that self-blocks the surviving set → DROP it. Note the **surviving active set excludes the explicit exception** `חיפוש עבודה נהג אוטובוס` (it is paused), so **עבודה is now `add` and CLEAR** (its only blocking positive is paused) — add it after the pauses land, exactly like מיניבוס. מיניבוס/מיניבוסים are `add` and CLEAR once the 8 minibus positives are paused (all in the pause set).

## STEP 3 — mutations (each: validateOnly → real → GAQL re-verify → log)
First generate the exact bodies:
```
node build-payloads.cjs      # writes payloads.generated.json
```
`payloads.generated.json` has these sections. **Execute in THIS order (a→h):**

**3a — create 4 new RSAs** (`rsa_creates`, one op each, endpoint `adGroupAds:mutate`). New ads are `status:ENABLED`; the campaign is still PAUSED so nothing serves until Step 3h. validateOnly each → real → re-read → log.
RSA creates trigger Google re-review (~1 business day → adStrength PENDING then hopefully GOOD); that's expected, not a failure.

**3b — REMOVE the 8 old ads** (`old_ad_removes`, one call, 8 remove ops, endpoint `adGroupAds:mutate`). Each op is `{"remove": "customers/1128064207/adGroupAds/<adGroupId>~<adId>"}`. Noam's order: delete the bad ads, do NOT merely pause. validateOnly → real → re-read (confirm status REMOVED) → log.

**3c — add 3 exact keywords** (`keyword_adds`, endpoint `adGroupCriteria:mutate`; all contain אוטובוס). validateOnly → real → re-read → log.

**3d — PAUSE the keywords** (`keyword_pauses_TEMPLATE`, endpoint `adGroupCriteria:mutate`, 31 ops = 30 non-אוטובוס + the `חיפוש עבודה נהג אוטובוס` exception). Each op's resourceName contains a `<CRIT_ID:...>` placeholder — replace it with the real `criterion_id` you matched by exact text from the Step-1 keyword pull (all are in group `202032774927`). If a listed text is not in the live pull (already removed), drop that op. If the live pull holds a non-אוטובוס keyword NOT in the template, ADD a pause op for it (the live non-אוטובוס set + the exception is the source of truth). validateOnly → real → re-read (confirm PAUSED) → log. **Confirm the 8 minibus positives AND `חיפוש עבודה נהג אוטובוס` are PAUSED before Step 3e** (their negatives depend on it).

**3e — add negatives** (`negative_adds`, endpoint `campaignCriteria:mutate`). BEFORE sending: remove any op whose keyword.text is already in the Step-1 itemized negative list (dedup — `add-if-absent` ones like אגד/רכבת/תחבורה ציבורית are likely already present), and remove any dropped by Step 2's self-block. **מיניבוס/מיניבוסים AND עבודה may be sent only after Step 3d confirmed their blocking positives are PAUSED** (the 8 minibus positives for מיניבוס; `חיפוש עבודה נהג אוטובוס` for עבודה — otherwise they self-block live positives). Send the survivors as ONE mutate call (or small batches). validateOnly → real → re-read → log.

**3f — CONDITIONAL bidding ceiling** (`campaign_bidding_ceiling_CONDITIONAL`, endpoint `campaigns:mutate`). **Apply ONLY if** the Step-0/Step-1 `campaign.bidding_strategy_type` == `TARGET_SPEND` (Maximize Clicks): send the op (sets `target_spend.cpc_bid_ceiling_micros = 8000000` = ₪8). **If it is anything else** (e.g. MANUAL_CPC, MAXIMIZE_CONVERSIONS, TARGET_CPA): DROP this op and REPORT the actual strategy to Noam — do not change bidding. **Never touch the budget** (stays ₪30/day). validateOnly → real → re-read → log (or log "skipped, strategy=<X>").

**HARD LAW reminders during Step 3:** old ads REMOVED not paused; every ENABLED headline must contain אוטובוס; no budget change; do not unpause PMax `24022824230`; מיניבוס negative only after minibus positives paused; no money action beyond the ₪8 ceiling (only if Maximize Clicks); message no one except the Step-5 notifications. **Do NOT enable the campaign yet — that is Step 3h, gated on Step 4.**

## STEP 4 — post-verify (the ENABLE gate)
Re-read the new ads and PROVE the bus-word rule holds on what actually landed:
```
SELECT ad_group.name, ad_group_ad.ad.id, ad_group_ad.status,
  ad_group_ad.ad.responsive_search_ad.headlines
FROM ad_group_ad WHERE campaign.id = 24022931741 AND ad_group_ad.status = 'ENABLED'
```
**ASSERTION:** every headline of every ENABLED RSA returned **contains the literal word אוטובוס**. Also confirm the 8 old ads are REMOVED and no ENABLED RSA remains from the old set.
- **If the assertion PASSES** → proceed to Step 3h (ENABLE).
- **If ANY enabled headline lacks אוטובוס, or an old ad is still enabled** → the ENABLE is BLOCKED. Pause the offending RSA, do **NOT** enable the campaign, and Telegram Noam:
  `node ~/.claude/hooks/tg-notify.js "Ads bus-fix: post-verify FAILED (a live headline lacks אוטובוס). Campaign left PAUSED, not enabled. Needs a look."`
  Then skip to Step 5's summary (state clearly it was NOT enabled) and STOP.

**3h — ENABLE the campaign (LAST mutation, only if Step 4 passed)** (`campaign_enable`, endpoint `campaigns:mutate`, `status:ENABLED`, updateMask `status`). validateOnly → real → re-read (confirm `campaign.status = ENABLED`) → log. This is the only campaign-status change in the run and it is explicitly approved by Noam (2026-07-23).

## STEP 4b — receipts + ops log
- Save the post-verify GAQL responses to `receipts-<TODAY>.json` in the cwd.
- Append ONE `run_summary` line (plus one line per mutation type) to `~/Claude/Architect/invoice-engine/ads-ops/ads-ops-log.jsonl` in the established schema:
  `{"ts":"<ISO8601>","action":"<run_summary|add_ads|remove_ad|add_keywords|pause_keyword|add_negative|set_bid_ceiling|enable_campaign>","detail":"...","verify":"..."}`
- Optional but nice: refresh `~/Claude/Architect/invoice-engine/ads-ops/ads.json` from a fresh full pull and scp to VPS (`scp ads.json vps:~/hud/data/ads.json`) so the HUD reflects the new state (campaign ENABLED, 31 keywords paused, old ads removed, new RSAs). If you skip it, say so.

## STEP 5 — report to Noam (the ONLY outbound messages)
1. Telegram summary — the campaign is LIVE:
   `node ~/.claude/hooks/tg-notify.js "Ads bus-fix pushed + campaign LIVE: 4 fresh RSAs (every headline has אוטובוס), 8 old ads deleted, +3 price keywords, 31 keywords paused, +N negatives incl מיניבוס. Budget ₪30/day$( [ bidding=MaximizeClicks ] && echo ', CPC cap ₪8' ). Watch the first day of spend."`
   (Fill N with the negatives actually added; include the "CPC cap ₪8" clause only if Step 3f applied it.)
2. FYI inbox rec (NOT an approval ask — the enable is already approved and done):
   `node ~/.claude/scripts/inbox-post.js rec "Google Ads bus campaign re-enabled (bus-fix live)" "2026-07-23: every headline now contains אוטובוס (4 fresh RSAs), 8 old ads deleted, 31 keywords paused (incl 1 job-seeker), מיניבוס + competitor/driver/out-of-geo negatives added. Campaign ENABLED, budget ₪30/day$( [ applied_ceiling ] && echo ', CPC cap ₪8/click' ). FYI only — monitor conversions." --target flags --lane noam --auto`

## Failure modes to expect
- **429 at Step 0** → abort + Telegram (Step 0 handles it). Most likely single failure.
- **validateOnly rejects an RSA** (e.g. a headline flagged) → log warn, skip that one RSA, continue others; report which. If the skipped RSA leaves a group with no compliant enabled ad, note it — post-verify still governs the enable.
- **v21 404 on a mutate** → retry same call on v23, note the bump.
- **criterion_id not found for a pause** → that keyword was already removed; drop the op, note it.
- **bidding_strategy_type not TARGET_SPEND** → skip Step 3f, report the actual strategy; do NOT change bidding.
- **post-verify fails** → campaign stays PAUSED, Telegram Noam (Step 4), do not enable.
- **composio-headless tools not exposed** (known intermittent headless gap) → if reads work but the WORKBENCH write path is missing, try native `GOOGLEADS_MUTATE_*` tools; if nothing writes, do NOT fake success — log the block, Telegram Noam, STOP (campaign stays PAUSED).
Expected clean-run duration: ~20–30 min (validate→real→re-verify round-trips across ~7 mutation groups + the enable gate).
