# PUSH-RUNBOOK — execute the Google Ads bus-fix (blind-executable)

You are a fresh session. This file + the JSON files beside it are your entire brief. Read this fully, then execute top to bottom. Everything you need (customer id, campaign/group/ad ids, GAQL, exact mutation bodies) is inlined or generated by the scripts here. **Do not improvise beyond this file.**

## What this run does
Replace the 8 old RSAs in dad's bus campaign with 4 fresh RSAs (one per ad group) where **every headline contains a bus word** (אוטובוס/הסעה/הסעות/מיניבוס), pause the 8 old ads, add 4 exact-match price keywords, pause 11 waste keywords, add the negatives that pass the self-block + dedup checks. **The campaign STAYS PAUSED the entire time.** Re-enabling it is a separate Noam-only money gate handled by an inbox ask at the very end.

## Fixed facts (do not re-derive)
- `customer_id` = **1128064207**
- Search campaign = **24022931741** ("אבי ורדי — חיפוש") — **PAUSED, keep it PAUSED**
- Ad groups: טיולים ואירועים=`197000680966` · הסעות לעובדים וכללי=`198995179115` · חתונות=`201158533351` · נתב״ג=`202032774927`
- 8 old ad resourceNames (adGroupId~adId), all to PAUSE:
  `197000680966~816419150547` · `197000680966~816438019647` · `198995179115~816438019650` · `198995179115~816454544560` · `201158533351~816438019644` · `201158533351~816527498297` · `202032774927~816438019641` · `202032774927~816454215061`
- Final URL for new RSAs: `https://www.avivardi.online`
- Working dir (cwd): `~/Claude/Avi-vardi/ads-pilot/changeset-2026-07-22/`

## Tool routes
- **READ (GAQL):** `mcp__composio-headless__GOOGLEADS_SEARCH_STREAM_GAQL`, param `customer_id="1128064207"`, one call per query. (Interactive fallback: `mcp__composio__COMPOSIO_MULTI_EXECUTE_TOOL` batching the same GAQL.)
- **WRITE (mutations):** `mcp__composio__COMPOSIO_REMOTE_WORKBENCH` → `proxy_execute(toolkit="googleads", method="POST", endpoint=<the _endpoint in the payload>, body=<the op body>)`. v21 is proven; if a call 404s specifically on v21, retry the identical call on v23 and note the bump. (Native fallback if the proxy is unavailable: `GOOGLEADS_MUTATE_AD_GROUP_ADS` / `GOOGLEADS_MUTATE_AD_GROUP_CRITERIA` / `GOOGLEADS_MUTATE_CAMPAIGN_CRITERIA` — but the REST bodies below are the primary path.)
- **Every mutation, no exceptions:** send with `"validateOnly": true` first (empty `{}` response = PASS, not failure) → then send the identical body without `validateOnly` → then an independent fresh GAQL re-read confirming it landed → then append one line to the ops log. If validateOnly fails: STOP that operation, log a `warn` alert, continue the rest.

---

## STEP 0 — quota probe (abort-early)
Run this GAQL:
```
SELECT campaign.id, campaign.status FROM campaign WHERE campaign.id = 24022931741
```
- On **429 / RESOURCE_EXHAUSTED**: quota still blocked. Do NOTHING else. Notify Noam and STOP:
  `node ~/.claude/hooks/tg-notify.js "Ads bus-fix push: quota still 429 at $(date). Not pushed — will retry next window."`
- On success: note the returned `campaign.status`. It MUST read `PAUSED`. **If it reads ENABLED**, STOP and Telegram Noam — someone re-enabled it; do not push onto a live campaign without his say. If PAUSED, continue.

## STEP 1 — archive pull (before any change)
Run these and save the combined JSON to `archive-<TODAY>.json` in the cwd (e.g. `archive-2026-07-23.json`):

```
-- current RSA content (for the archive + the bus-word audit)
SELECT ad_group.name, ad_group_ad.ad.id, ad_group_ad.status,
  ad_group_ad.ad.responsive_search_ad.headlines, ad_group_ad.ad.responsive_search_ad.descriptions
FROM ad_group_ad WHERE campaign.id = 24022931741

-- itemized live negatives (dedup source for Step 3d)
SELECT campaign_criterion.criterion_id, campaign_criterion.keyword.text, campaign_criterion.keyword.match_type
FROM campaign_criterion WHERE campaign.id = 24022931741 AND campaign_criterion.type = 'KEYWORD'

-- live positive keywords WITH criterion_id (needed to map pause targets + re-run self-block)
SELECT ad_group.name, ad_group.id, ad_group_criterion.criterion_id,
  ad_group_criterion.keyword.text, ad_group_criterion.keyword.match_type, ad_group_criterion.status
FROM keyword_view WHERE campaign.id = 24022931741
```
**AUDIT (write into your final report):** from the first query, list which OLD headlines did NOT contain any of אוטובוס/הסעה/הסעות/מיניבוס — that's the "before" evidence for Noam that the bus-word rule was being violated.

## STEP 2 — re-run the self-block check against the FRESH keyword pull
Do not trust the snapshot. Build the live positive-keyword text list from the Step-1 keyword pull, then for EACH negative candidate in `negatives.json` with decision `add`/`add-if-absent`, substring-test it against that fresh list. Fastest path — drop the fresh keyword texts into a file and reuse the shipped checker logic:
```
# save the fresh positive keyword texts (one JSON array) to fresh-positives.json in cwd, then:
node -e '
const fs=require("fs");
const pos=JSON.parse(fs.readFileSync("fresh-positives.json","utf8"));           // array of live keyword.text strings
const negs=JSON.parse(fs.readFileSync("negatives.json","utf8"));
for(const n of negs.filter(x=>x.decision!=="skip")){
  const hits=pos.filter(p=>p.includes(n.text));
  console.log((hits.length?"SELF-BLOCK->DROP ":"CLEAR         ")+n.text+(hits.length?"  hits:"+hits.join(","):""));
}'
```
Any candidate that now shows SELF-BLOCK → DROP it from the negative adds (a keyword may have been added since the snapshot). Candidates marked `skip` in the file stay skipped (מיניבוס is permanent).

## STEP 3 — mutations (each: validateOnly → real → GAQL re-verify → log)
First generate the exact bodies:
```
node build-payloads.cjs      # writes payloads.generated.json
```
`payloads.generated.json` has 5 sections. Execute in THIS order:

**3a — create 4 new RSAs** (`payloads.generated.json.rsa_creates`, one op each, endpoint `adGroupAds:mutate`). New ads are `status:ENABLED` but the campaign is PAUSED so nothing serves. validateOnly each → real → re-read (query below) → log.
RSA creates trigger Google re-review (~1 business day → adStrength PENDING then hopefully GOOD); that's expected, not a failure.

**3b — pause the 8 old ads** (`payloads.generated.json.old_ad_pauses`, one call, 8 update ops, endpoint `adGroupAds:mutate`). Never remove — PAUSE only. validateOnly → real → re-read → log.

**3c — add 4 exact keywords** (`payloads.generated.json.keyword_adds`, endpoint `adGroupCriteria:mutate`). validateOnly → real → re-read → log.

**3d — add negatives** (`payloads.generated.json.negative_adds`, endpoint `campaignCriteria:mutate`). BEFORE sending: remove any op whose keyword.text is already in the Step-1 itemized negative list (dedup), and remove any dropped by Step 2. `add-if-absent` ops in particular are likely already present (אגד/רכבת/תחבורה ציבורית) — dedup them out. Send the survivors as ONE mutate call (or small batches). validateOnly → real → re-read → log.

**3e — pause 11 waste keywords** (`payloads.generated.json.keyword_pauses_TEMPLATE`, endpoint `adGroupCriteria:mutate`). Each op's resourceName contains a `<CRIT_ID:...>` placeholder — replace it with the real `criterion_id` you matched by exact text from the Step-1 keyword pull (all 11 are in group `202032774927`). If a listed text is not found in the live pull (already removed), drop that op. validateOnly → real → re-read → log.

**HARD LAW reminders during Step 3:** no campaign status change of any kind (no ENABLE — that is Noam's tap only); no budget/bid change; do not unpause PMax `24022824230`; old ads PAUSED not removed; no money action; message no one except the two Noam-facing notifications in this file.

## STEP 4 — post-verify + receipts
Re-read the new ads and PROVE the bus-word rule holds on what actually landed:
```
SELECT ad_group.name, ad_group_ad.ad.id, ad_group_ad.status,
  ad_group_ad.ad.responsive_search_ad.headlines
FROM ad_group_ad WHERE campaign.id = 24022931741 AND ad_group_ad.status = 'ENABLED'
```
For every ENABLED RSA headline returned, assert it contains one of אוטובוס/הסעה/הסעות/מיניבוס. If ANY headline lacks a bus word, that RSA create is wrong — pause it and flag; do not leave a non-compliant ad enabled.
- Save the post-verify GAQL responses to `receipts-<TODAY>.json` in the cwd.
- Append ONE `run_summary` line (plus one line per mutation type) to `~/Claude/Architect/invoice-engine/ads-ops/ads-ops-log.jsonl` in the established schema:
  `{"ts":"<ISO8601>","action":"<run_summary|add_ads|pause_ad|add_keywords|add_negative>","detail":"...","verify":"..."}`
- Optional but nice: refresh `~/Claude/Architect/invoice-engine/ads-ops/ads.json` from a fresh full pull and scp to VPS (`scp ads.json vps:~/hud/data/ads.json`) so the HUD reflects the new state. If you skip it, say so.

## STEP 5 — report to Noam (the ONLY outbound messages)
1. Telegram summary:
   `node ~/.claude/hooks/tg-notify.js "Ads bus-fix pushed: 4 new RSAs live (all headlines bus-worded), 8 old ads paused, +4 price keywords, +N negatives, 11 waste keywords paused. Campaign still PAUSED — awaiting your re-enable."`
2. Money-gate inbox ask (re-enable spend is Noam's decision, never automated):
   `node ~/.claude/scripts/inbox-post.js ask "Re-enable Google Ads bus campaign?" "Bus-fix pushed 2026-07-23: every headline now contains a bus word (4 fresh RSAs), waste keywords + competitor/driver/out-of-geo negatives tightened. Daily budget stays ₪30. Campaign is PAUSED — tap Approve to re-enable spend." --lane noam`

## Failure modes to expect
- **429 at Step 0** → abort + Telegram (Step 0 handles it). Most likely single failure.
- **validateOnly rejects an RSA** (e.g. a headline flagged) → log warn, skip that one RSA, continue others; report which.
- **v21 404 on a mutate** → retry same call on v23, note the bump.
- **criterion_id not found for a pause** → that keyword was already removed; drop the op, note it.
- **composio-headless tools not exposed** (known intermittent headless gap) → if reads work but the WORKBENCH write path is missing, try native `GOOGLEADS_MUTATE_*` tools; if nothing writes, do NOT fake success — log the block, Telegram Noam, STOP.
Expected clean-run duration: ~15–25 min (mostly the validate→real→re-verify round-trips ×5 mutation groups).
